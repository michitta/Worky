declare class SendChatActionParam {
    private _chatId;
    private _action;
    constructor(params: {
        chatId: number | string;
        action: string;
    });
    get chatId(): number | string;
    get action(): string;
}
export default SendChatActionParam;
