import InputFile from '../entities/input_file';
declare class SetChatPhotoParam {
    private _chatId;
    private _photo;
    constructor(params: {
        chatId: number | string;
        photo: InputFile;
    });
    get chatId(): number | string;
    get photo(): InputFile;
}
export default SetChatPhotoParam;
