declare class PollOption {
    private _text;
    private _voterCount;
    constructor(params: {
        text: string;
        voterCount: number;
    });
    get text(): string;
    get voterCount(): number;
}
export default PollOption;
