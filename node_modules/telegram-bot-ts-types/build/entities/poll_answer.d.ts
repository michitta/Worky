import User from './user';
declare class PollAnswer {
    private _pollId;
    private _user;
    private _optionIds;
    constructor(params: {
        pollId: string;
        user: User;
        optionIds: number[];
    });
    get pollId(): string;
    get user(): User;
    get optionIds(): number[];
}
export default PollAnswer;
