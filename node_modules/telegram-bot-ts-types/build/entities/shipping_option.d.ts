import LabeledPrice from './labeled_price';
declare class ShippingOption {
    private _id;
    private _title;
    private _prices;
    constructor(params: {
        id: string;
        title: string;
        prices: LabeledPrice[];
    });
    get id(): string;
    get title(): string;
    get prices(): LabeledPrice[];
}
export default ShippingOption;
