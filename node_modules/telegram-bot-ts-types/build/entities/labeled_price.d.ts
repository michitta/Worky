declare class LabeledPrice {
    private _label;
    private _amount;
    constructor(params: {
        label: string;
        amount: number;
    });
    get label(): string;
    get amount(): number;
}
export default LabeledPrice;
