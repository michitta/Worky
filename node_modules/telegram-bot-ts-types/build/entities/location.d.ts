declare class Location {
    private _longitude;
    private _latitude;
    constructor(params: {
        longitude: number;
        latitude: number;
    });
    get longitude(): number;
    get latitude(): number;
}
export default Location;
