declare class EncryptedCredentials {
    private _data;
    private _hash;
    private _secret;
    constructor(params: {
        data: string;
        hash: string;
        secret: string;
    });
    get data(): string;
    get hash(): string;
    get secret(): string;
}
export default EncryptedCredentials;
