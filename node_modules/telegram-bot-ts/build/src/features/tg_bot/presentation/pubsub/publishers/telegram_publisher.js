"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var TelegramPublisher = /** @class */ (function () {
    function TelegramPublisher() {
        this.subscribers = [];
    }
    TelegramPublisher.prototype.subscribe = function (telegramSubscriber) {
        this.subscribers.push(telegramSubscriber);
    };
    TelegramPublisher.prototype.unsubscribe = function (telegramSubscriber) {
        var index = this.subscribers.indexOf(telegramSubscriber);
        if (index !== -1) {
            this.subscribers.splice(index, 1);
        }
    };
    TelegramPublisher.prototype.publish = function (update) {
        for (var _i = 0, _a = this.subscribers; _i < _a.length; _i++) {
            var subscriber = _a[_i];
            subscriber.update(update);
        }
    };
    return TelegramPublisher;
}());
exports.default = TelegramPublisher;
//# sourceMappingURL=telegram_publisher.js.map