import { Failure } from "../../../../core/failures";
import { ApiClient } from "./telegram_client";
import { User } from "telegram-bot-ts-types";
interface ApiUserRemoteDatasource {
    getMe(): Promise<User | Failure>;
}
declare class ApiUserRemoteDatasourceImpl implements ApiUserRemoteDatasource {
    private client;
    constructor(client: ApiClient);
    private checkResponse;
    getMe(): Promise<User | Failure>;
}
export { ApiUserRemoteDatasource, ApiUserRemoteDatasourceImpl };
