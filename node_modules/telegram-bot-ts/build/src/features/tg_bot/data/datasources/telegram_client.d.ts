import { Failure } from "../../../../core/failures";
import FormData from "formdata-node";
interface ApiClient {
    token: string;
    execute(command: string, params?: {
        [key: string]: any;
    }): Promise<string | Failure>;
}
declare class TelegramApiClient implements ApiClient {
    token: string;
    constructor(token: string);
    execute(command: string, paramsFormData?: FormData): Promise<string | Failure>;
    private encodeParams;
}
export { ApiClient, TelegramApiClient };
